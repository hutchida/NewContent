# NewContent

Loops through metadata dump of xml content items finding docs created and published within the last week. Produces graphs and tables based on content types and practice areas then exports to basic html page. Uses pandas, matplotlib, beautifulsoup. 
